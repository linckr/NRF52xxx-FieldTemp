# E104-BT5010A 温湿度气压计项目优化方案

> 分析日期：2026-01-30  
> 目标文件：`src/main.c` (约2913行)  
> 硬件平台：nRF52810 (194KB Flash, 24KB RAM)  
> SDK版本：NCS 3.2.1

---

## 一、代码架构与模块化优化

### 1.1 单文件代码过于庞大

当前所有代码都在一个 `main.c` 文件中（约3000行），建议拆分为多个模块：

| 模块文件 | 功能描述 |
|----------|----------|
| `sensors/aht30.c/h` | AHT30 温湿度传感器驱动 |
| `sensors/spl06.c/h` | SPL06-001 气压传感器驱动 |
| `storage/w25q64.c/h` | W25Q64 SPI Flash 驱动 |
| `storage/nvs_config.c/h` | NVS 配置管理 |
| `ble/ble_services.c/h` | 蓝牙GATT服务定义 |
| `ble/ble_adv.c/h` | 蓝牙广播管理 |
| `led.c/h` | LED指示灯控制 |

### 1.2 全局变量过多

当前有约50+个静态全局变量，建议使用结构体封装相关变量：

```c
// 优化前：分散的全局变量
static bool history_transfer_active = false;
static uint16_t history_transfer_sector = 0;
static uint16_t history_transfer_record_idx = 0;
static uint32_t history_start_timestamp = 0;
static bool history_binary_search_done = false;
static uint32_t history_total_sent_packets = 0;
static uint32_t history_total_sent_records = 0;
static uint32_t history_last_sent_timestamp = 0;

// 优化后：结构体封装
typedef struct {
    uint16_t sector;
    uint16_t record_idx;
    uint32_t start_timestamp;
    bool active;
    bool binary_search_done;
    uint32_t total_sent_packets;
    uint32_t total_sent_records;
    uint32_t last_sent_timestamp;
} history_transfer_ctx_t;

static history_transfer_ctx_t history_ctx;
```

### 1.3 模块化实施状态（2026-01-30 已完成）

✅ **已创建的模块文件：**

```
src/
├── main.c                    # 主程序（保留 GATT 服务定义）
├── common.h                  # 公共数据类型定义
├── led.c/h                   # LED 指示灯控制
├── sensors/
│   ├── aht30.c/h            # AHT30 温湿度传感器驱动
│   └── spl06.c/h            # SPL06-001 气压传感器驱动
├── storage/
│   ├── w25q64.c/h           # W25Q64 SPI Flash 驱动
│   └── nvs_config.c/h       # NVS 配置管理
└── ble/
    ├── ble_adv.c/h          # 蓝牙广播管理
    └── ble_services.c/h     # 蓝牙 GATT 服务辅助函数
```

✅ **已封装的结构体（定义在 `common.h`）：**

| 结构体 | 用途 |
|--------|------|
| `history_transfer_ctx_t` | 历史数据传输上下文 |
| `time_sync_ctx_t` | 时间同步上下文 |
| `clear_data_ctx_t` | 清空数据上下文 |
| `bt_state_ctx_t` | 蓝牙状态上下文 |
| `device_config_ctx_t` | 设备配置上下文（在 `nvs_config.h`）|
| `storage_position_t` | 存储位置上下文（在 `nvs_config.h`）|

### 1.4 模块化实施注意事项

⚠️ **重要提醒：**

1. **GATT 服务定义必须保留在 main.c**
   - Zephyr 的 `BT_GATT_SERVICE_DEFINE` 宏需要在编译时静态定义
   - GATT 回调函数与服务定义紧密耦合，不能轻易分离
   - 如需完全分离，需要重新设计服务注册机制

2. **模块间依赖关系**
   - `main.c` 依赖所有模块的头文件
   - 传感器模块相互独立
   - `nvs_config` 依赖 `device_config_ctx_t` 和 `storage_position_t` 结构体
   - `ble_services` 依赖 `common.h` 中的 `history_transfer_ctx_t`

3. **编译配置**
   - 已更新 `CMakeLists.txt` 添加所有新源文件
   - 添加了 `target_include_directories(app PRIVATE src)` 以支持模块间引用

4. **后续整合步骤**
   - 需要修改 `main.c` 以使用新模块的 API
   - 替换原有的函数调用为模块函数
   - 删除 `main.c` 中已迁移到模块的代码
   - 编译测试验证功能正常

5. **Flash/RAM 影响**
   - 模块化本身不会显著增加 Flash/RAM 占用
   - 每个 `.c` 文件增加少量符号表开销
   - 预计 Flash 增加 < 500 字节

---

## 二、内存优化（重要：nRF52810只有24KB RAM）

### 2.1 静态缓冲区优化

```c
// 当前：W25Q64读取使用256+4字节静态缓冲区
static uint8_t tx_buf_data[4 + 256];  // 260字节
static uint8_t rx_buf_data[4 + 256];  // 260字节
// 共520字节

// 建议：根据实际最大读取长度调整（大部分操作只读12字节记录）
#define W25Q64_READ_BUF_SIZE (4 + 64)  // 68字节够用
static uint8_t tx_buf_data[W25Q64_READ_BUF_SIZE];
static uint8_t rx_buf_data[W25Q64_READ_BUF_SIZE];
// 可节省约380字节RAM
```

### 2.2 RAM缓冲区大小调整

```c
// 当前
#define RAM_BUFFER_SIZE 5
static struct data_record ram_buffer[RAM_BUFFER_SIZE];  // 5 * 12 = 60字节

// 建议：考虑增加到8条，提高Flash写入效率（减少擦写次数）
#define RAM_BUFFER_SIZE 8  // 8 * 12 = 96字节
```

### 2.3 printk日志过多

大量 `printk` 调试信息会占用Flash空间且影响实时性，建议添加日志级别控制：

```c
// 日志级别定义
#define LOG_LEVEL_DEBUG 0
#define LOG_LEVEL_INFO  1
#define LOG_LEVEL_WARN  2
#define LOG_LEVEL_ERROR 3

#ifndef CURRENT_LOG_LEVEL
#define CURRENT_LOG_LEVEL LOG_LEVEL_INFO  // 发布版本设为WARN或ERROR
#endif

#define LOG_DEBUG(fmt, ...) do { \
    if (CURRENT_LOG_LEVEL <= LOG_LEVEL_DEBUG) printk("[D] " fmt, ##__VA_ARGS__); \
} while(0)

#define LOG_INFO(fmt, ...) do { \
    if (CURRENT_LOG_LEVEL <= LOG_LEVEL_INFO) printk("[I] " fmt, ##__VA_ARGS__); \
} while(0)

#define LOG_WARN(fmt, ...) do { \
    if (CURRENT_LOG_LEVEL <= LOG_LEVEL_WARN) printk("[W] " fmt, ##__VA_ARGS__); \
} while(0)

#define LOG_ERROR(fmt, ...) do { \
    if (CURRENT_LOG_LEVEL <= LOG_LEVEL_ERROR) printk("[E] " fmt, ##__VA_ARGS__); \
} while(0)
```

---

## 三、功耗优化（关键优化点）

### 3.1 I2C电源控制被注释掉

当前代码（第344-367行）I2C功耗控制被 `#if 0` 禁用，建议启用：

```c
// I2C 电源控制状态
static bool i2c_power_enabled = false;

// 启用 I2C 外设
static void i2c_power_enable(void)
{
    if (!i2c_power_enabled) {
        nrf_twim_enable(NRF_TWIM0);
        i2c_power_enabled = true;
        k_busy_wait(10);  // 10 微秒等待稳定
    }
}

// 禁用 I2C 外设
static void i2c_power_disable(void)
{
    if (i2c_power_enabled) {
        nrf_twim_disable(NRF_TWIM0);
        i2c_power_enabled = false;
    }
}
```

**预期收益**：I2C空闲时功耗从约100μA降至<1μA

### 3.2 SPL06传感器模式优化

当前 SPL06 配置为连续测量模式（功耗较高）：

```c
// 当前配置（第621行）- 连续模式，功耗约3.2mA
uint8_t meas_cfg[2] = {SPL06_REG_MEAS_CFG, 0x07};  // bit[2:0] = 111

// 建议改为按需单次测量模式
// 待机模式 + 单次触发
uint8_t meas_cfg[2] = {SPL06_REG_MEAS_CFG, 0x00};  // 待机模式

// 需要读取时触发单次测量
static int spl06_trigger_measurement(void)
{
    uint8_t cmd[2] = {SPL06_REG_MEAS_CFG, 0x02};  // 单次压力+温度测量
    return i2c_write(i2c_dev, cmd, 2, SPL06_I2C_ADDR);
}
```

**预期收益**：传感器待机功耗从3.2mA降至<1μA

### 3.3 采集间隔优化

```c
// 当前默认1秒采集一次，功耗较高
#define DEFAULT_SAMPLE_INTERVAL  1

// 建议根据应用场景调整
#define DEFAULT_SAMPLE_INTERVAL  60  // 1分钟采集一次

// 或者实现动态调整：
// - 温度变化快时：短间隔（如10秒）
// - 温度稳定时：长间隔（如5分钟）
```

### 3.4 间歇广播优化

当前间歇广播10秒开/10秒关，可进一步优化：

```c
// 建议：分阶段广播策略
// 阶段1：启动后30秒快速广播（便于发现）
// 阶段2：慢速广播30秒后进入间歇模式
// 阶段3：间歇模式 - 广播5秒，休眠55秒（1分钟周期）

#define INTERMITTENT_ADV_ON_SEC   5   // 广播5秒
#define INTERMITTENT_ADV_OFF_SEC  55  // 休眠55秒

// 在 adv_stop_work_handler 中修改
k_work_schedule(&adv_start_work, K_SECONDS(INTERMITTENT_ADV_OFF_SEC));
```

**预期收益**：广播功耗降低约90%

### 3.5 DCDC模式确认

确保已启用DCDC转换器（在 `prj.conf` 中）：

```ini
# 启用DCDC（降低功耗）
CONFIG_SOC_DCDC_NRF52X=y
```

---

## 四、蓝牙通信优化

### 4.1 MTU协商

当前代码没有请求MTU协商，使用默认23字节：

```c
// 添加MTU交换回调
static void mtu_exchange_cb(struct bt_conn *conn, uint8_t err,
                            struct bt_gatt_exchange_params *params)
{
    if (err) {
        printk("[BLE] MTU交换失败: %d\r\n", err);
    } else {
        printk("[BLE] MTU交换成功: %d\r\n", bt_gatt_get_mtu(conn));
    }
}

static struct bt_gatt_exchange_params mtu_exchange_params = {
    .func = mtu_exchange_cb,
};

// 在连接回调中请求MTU交换
static void bt_connected_cb(struct bt_conn *conn, uint8_t err)
{
    if (!err) {
        bt_connected = true;
        // 请求更大MTU（nRF52810支持最大247字节）
        bt_gatt_exchange_mtu(conn, &mtu_exchange_params);
        // ... 其他代码
    }
}
```

### 4.2 历史数据传输动态调整

根据协商后的MTU动态计算每包记录数：

```c
// 当前固定5条
#define STREAM_RECORDS_PER_PACKET 5

// 建议动态计算
static uint8_t get_max_records_per_packet(struct bt_conn *conn)
{
    uint16_t mtu = bt_gatt_get_mtu(conn);
    uint16_t payload = mtu - 3;  // ATT header 3字节
    return payload / W25Q64_RECORD_SIZE;  // 12字节/记录
}
// MTU=247时，可发送20条记录/包，效率提升4倍
```

### 4.3 连接参数优化

```c
// 建议在连接后请求更优的连接参数
static const struct bt_le_conn_param conn_param = {
    .interval_min = 80,   // 100ms (80 * 1.25ms)
    .interval_max = 160,  // 200ms
    .latency = 4,         // 从机延迟，可跳过4个连接事件
    .timeout = 400,       // 4秒超时
};

// 在连接回调中更新
bt_conn_le_param_update(conn, &conn_param);
```

**预期收益**：连接态功耗降低约50%

---

## 五、传感器驱动优化

### 5.1 AHT30 CRC校验

当前读取数据未校验CRC（第479行）：

```c
// 添加CRC8校验函数
static uint8_t crc8_aht(const uint8_t *data, uint8_t len)
{
    uint8_t crc = 0xFF;
    for (uint8_t i = 0; i < len; i++) {
        crc ^= data[i];
        for (uint8_t j = 0; j < 8; j++) {
            crc = (crc & 0x80) ? (crc << 1) ^ 0x31 : crc << 1;
        }
    }
    return crc;
}

// 在 aht30_read() 中添加校验
static int aht30_read(int32_t *temperature, int32_t *humidity)
{
    // ... 读取7字节数据 ...
    
    // 校验CRC
    uint8_t crc = crc8_aht(data, 6);
    if (crc != data[6]) {
        printk("AHT30 CRC校验失败: 计算=0x%02X, 接收=0x%02X\r\n", crc, data[6]);
        return -EIO;
    }
    
    // ... 解析数据 ...
}
```

### 5.2 SPL06校准系数验证

```c
// 在 spl06_init() 加载校准系数后添加验证
if (spl06_calib.c0 == 0 && spl06_calib.c1 == 0 && 
    spl06_calib.c00 == 0 && spl06_calib.c10 == 0) {
    printk("SPL06 警告: 校准系数全为0，传感器可能未正确初始化\r\n");
    return -EINVAL;
}

// 范围检查（根据datasheet典型值）
if (abs(spl06_calib.c0) > 2000 || abs(spl06_calib.c1) > 2000) {
    printk("SPL06 警告: 校准系数c0/c1超出合理范围\r\n");
}
```

### 5.3 错误重试机制

```c
// 添加带重试的传感器读取函数
static int sensor_read_with_retry(
    int (*read_func)(int32_t*, int32_t*),
    int32_t *val1, int32_t *val2,
    int max_retries, int delay_ms)
{
    for (int i = 0; i < max_retries; i++) {
        int ret = read_func(val1, val2);
        if (ret == 0) {
            return 0;
        }
        if (i < max_retries - 1) {
            k_msleep(delay_ms);
        }
    }
    return -EIO;
}

// 使用示例
int ret = sensor_read_with_retry(aht30_read, &temp, &hum, 3, 10);
```

---

## 六、存储系统优化

### 6.1 Flash擦写次数跟踪

```c
// 添加擦写计数（用于监控Flash寿命）
// W25Q64 擦写寿命：100,000次
static uint32_t total_erase_count = 0;
#define NVS_ID_ERASE_COUNT  6  // NVS存储key

static int w25q64_sector_erase(uint32_t addr)
{
    // ... 擦除操作 ...
    
    total_erase_count++;
    
    // 每1000次保存一次计数到NVS
    if (total_erase_count % 1000 == 0) {
        nvs_write(&nvs_fs, NVS_ID_ERASE_COUNT, 
                  &total_erase_count, sizeof(total_erase_count));
        printk("[存储] Flash累计擦写: %u 次\r\n", total_erase_count);
    }
    
    // 寿命告警（达到80%寿命时告警）
    if (total_erase_count == 80000) {
        printk("[存储] 警告: Flash擦写次数已达80%%寿命!\r\n");
    }
    
    return 0;
}
```

### 6.2 扇区切换告警

```c
// 第3303行附近：环形缓冲区满时添加告警
if (next_sector == oldest_sector && next_record_in_sector > 0) {
    printk("[存储] 警告: 存储即将满，将覆盖最旧数据 (扇区 %d)\r\n", oldest_sector);
    
    // 可选：通过蓝牙状态特性通知App
    if (bt_connected && status_char_attr != NULL) {
        // 在状态字节中添加存储满标志
        status_char_data[4] |= 0x08;  // bit3 = 存储满告警
        bt_gatt_notify(NULL, status_char_attr, status_char_data, sizeof(status_char_data));
    }
}
```

### 6.3 写入数据压缩（可选/->废弃）

当前每条记录12字节，可考虑压缩：

```c
// 当前格式（12字节）
struct data_record {
    uint32_t timestamp;          // 4字节
    int16_t temperature;         // 2字节
    uint16_t humidity;           // 2字节
    uint32_t pressure_centihpa;  // 4字节
} __packed;

// 压缩格式（8字节）- 可选方案
struct data_record_compact {
    uint16_t time_offset;        // 2字节：相对于基准时间的偏移（秒），最大18小时
    int16_t temperature;         // 2字节
    uint16_t humidity;           // 2字节
    uint16_t pressure_dhpa;      // 2字节：0.1hPa精度
} __packed;
// 节省33%存储空间
```

---

## 七、代码质量优化

### 7.1 魔数替换为宏定义

```c
// 第825行
if (saved_timestamp > 946684800) {  // 魔数

// 优化为
#define TIMESTAMP_YEAR_2000 946684800UL  // 2000-01-01 00:00:00 UTC
#define TIMESTAMP_YEAR_2020 1577836800UL // 2020-01-01 00:00:00 UTC

if (saved_timestamp > TIMESTAMP_YEAR_2000) {
```

### 7.2 嵌套函数改为标准函数

```c
// 第739行：使用了GNU扩展的auto嵌套函数（非标准C）
auto int64_t mul_q(int64_t a_q, int64_t b_q) {
    return (a_q * b_q) >> s;
}

// 优化为标准C内联函数
static inline int64_t mul_q20(int64_t a_q, int64_t b_q)
{
    return (a_q * b_q) >> SPL06_FP_SHIFT;
}
```

### 7.3 未使用代码清理

需要清理的代码：

| 位置 | 内容 | 建议 |
|------|------|------|
| 第3416行 | `storage_read_history()` 空函数 | 删除 |
| 第88行 | `time_sync_service_uuid` 未使用 | 删除 |
| 第296-297行 | 废弃注释 | 删除 |
| 第344-367行 | `#if 0` 禁用代码 | 启用或删除 |

### 7.4 GPIO操作封装

```c
// 当前：直接操作寄存器
LED_PORT->OUTCLR = DATA_LED_MASK;
LED_PORT->OUTSET = LINK_LED_MASK;

// 优化为内联函数
static inline void led_on(uint32_t mask) 
{ 
    LED_PORT->OUTCLR = mask;  // 低电平点亮
}

static inline void led_off(uint32_t mask) 
{ 
    LED_PORT->OUTSET = mask;  // 高电平熄灭
}

static inline void led_toggle(uint32_t mask) 
{ 
    LED_PORT->OUT ^= mask;
}

// 使用
led_on(DATA_LED_MASK);
led_off(LINK_LED_MASK);
```

---

## 八、安全性优化

### 8.1 蓝牙配对/绑定（可选）

当前没有实现配对机制，数据可能被未授权设备读取：

```c
// 添加配对支持（需要在prj.conf中启用）
// CONFIG_BT_SMP=y

// 设置IO能力（无显示无输入）
static void auth_cancel(struct bt_conn *conn)
{
    printk("[BLE] 配对取消\r\n");
}

static struct bt_conn_auth_cb auth_callbacks = {
    .cancel = auth_cancel,
};

// 在main()中注册
bt_conn_auth_cb_register(&auth_callbacks);

// 在连接后请求加密
bt_conn_set_security(conn, BT_SECURITY_L2);
```

### 8.2 时间戳防篡改

```c
// 添加时间戳合理性检查
static bool validate_timestamp(uint32_t ts)
{
    // 检查是否在合理范围内（2020-2050年）
    if (ts < TIMESTAMP_YEAR_2020 || ts > 2524608000UL) {
        return false;
    }
    
    // 检查是否比当前时间晚太多（超过1小时视为异常）
    uint32_t current = get_current_timestamp();
    if (current > 0 && ts > current + 3600) {
        return false;
    }
    
    return true;
}
```

---

## 九、优化实施优先级

### 高优先级（立即实施，收益明显）

| 序号 | 优化项 | 预期收益 | 工作量 |
|------|--------|----------|--------|
| 1 | SPL06改为单次测量模式 | 功耗降低3mA | 小 |
| 2 | 启用I2C电源控制 | 待机功耗降低 | 小 |
| 3 | 调整默认采集间隔为60秒 | 电池寿命延长60倍 | 极小 |
| 4 | 添加MTU协商 | 历史传输加速4倍 | 小 |
| 5 | AHT30添加CRC校验 | 数据可靠性提升 | 小 |

### 中优先级（计划实施）

| 序号 | 优化项 | 预期收益 | 工作量 |
|------|--------|----------|--------|
| 6 | 代码模块化拆分 | 可维护性大幅提升 | 大 |
| 7 | 连接参数优化 | 连接态功耗降低50% | 小 |
| 8 | 间歇广播参数调整 | 广播功耗降低90% | 极小 |
| 9 | 添加日志级别控制 | Flash空间节省 | 中 |
| 10 | 全局变量结构体封装 | 代码可读性提升 | 中 |

### 低优先级（有空再做）

| 序号 | 优化项 | 预期收益 | 工作量 |
|------|--------|----------|--------|
| 11 | 清理未使用代码 | 代码整洁 | 小 |
| 12 | 魔数替换为宏 | 可读性提升 | 小 |
| 13 | Flash擦写计数 | 寿命监控 | 小 |
| 14 | 数据压缩存储 | 存储容量+50% | 大 |
| 15 | 蓝牙配对支持 | 安全性提升 | 中 |

---

## 十、功耗预估对比

| 状态 | 优化前 | 优化后 | 降低比例 |
|------|--------|--------|----------|
| 深度睡眠 | ~5μA | ~3μA | 40% |
| 待机（广播） | ~500μA | ~50μA | 90% |
| 采集中 | ~8mA | ~5mA | 37% |
| 连接态空闲 | ~200μA | ~100μA | 50% |
| 历史传输 | ~10mA | ~8mA | 20% |

**预计电池寿命**（以CR2032 220mAh计算）：
- 优化前：约7天（1秒采集+持续广播）
- 优化后：约180天（60秒采集+间歇广播）

---

## 附录：参考资料

1. nRF52810 Product Specification v1.3
2. AHT30 Datasheet
3. SPL06-001 Datasheet
4. W25Q64JV Datasheet
5. Zephyr RTOS Power Management Guide
6. Nordic DevZone - Low Power BLE Design
